require('neo-tree').setup({
        filesystem = {
            filtered_items = {
				hide_dotfiles = false,
                visible = true,
            },
        },
window = {
            width = 30,  -- задаёт ширину панели, можно уменьшить или увеличить
        },
    })
